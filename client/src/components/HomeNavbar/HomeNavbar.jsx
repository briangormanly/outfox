import React from 'react';

import { Nav, NavContainer } from './HomeNavbar.elements';

const HomeNavbar = () => {
	return (
		<Nav>
			<NavContainer>
				<LogoContainer />
			</NavContainer>
		</Nav>
	);
};

export default HomeNavbar;
